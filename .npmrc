# Prevent npm from creating package-lock.json
# This project uses pnpm, which creates pnpm-lock.yaml instead
package-lock=false

# Optional: Enforce pnpm usage (will error if npm or yarn is used)
# Uncomment the line below to strictly enforce pnpm
engine-strict=true
